apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: simple-node-app
  namespace: argocd
spec:
  project: default

  source:
    repoURL: 'https://github.com/vinatu/simple-node-app.git'
    targetRevision: HEAD
    path: .

  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
